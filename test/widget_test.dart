import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    // Kör inte full-app widget tests här ännu eftersom main.dart kräver
    // Firebase.initializeApp() + native bindings.
    expect(true, isTrue);
  });
}
